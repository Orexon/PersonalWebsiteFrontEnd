import { Container } from "@material-ui/core";
import React from "react";
import "./Home.css";

const Home = () => {
    return (
        <Container className="home" id="HomeContainer">
            <div></div>
            {/* <HomeBackground>
                <VideoBg autoPlay loop muted src={Video} type="video/mp4" />
            </HomeBackground> */}
        </Container>
    );
};

export default Home;
